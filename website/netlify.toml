[build]
  base = "website/"
  command = "npm run build"
  publish = "dist/"

[dev]
  command = "npm start"
  targetPort = 3000
  framework = "#custom"

[[redirects]]
  from = "/*"
  to = "/graphgen/:splat"